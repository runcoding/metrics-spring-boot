#debug: true
spring:
  profiles:
    active: dev

server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
    max-threads:  10

logging:
  level:
    org.springframework:  info

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false